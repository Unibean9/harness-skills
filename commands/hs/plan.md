---
description: Turn a chosen approach into concrete steps, files to change, and completion criteria.
argument-hint: "[task]"
---

Invoke `hs:plan` with `$ARGUMENTS`. Produce plans only; do not implement application code.
