Read and follow `AGENTS.md` at the repo root — that's the harness instruction
for this project. For Gemini-specific optional hooks and their coverage limits,
use the compatibility matrix in `README.md`.
