{
  "name": "harness-skills",
  "version": "2.0.0",
  "private": true,
  "engines": { "node": ">=22" },
  "bin": { "hs": "bin/hs.mjs" },
  "files": ["bin", "scripts", "skills", "hooks", "agents", "docs", "README.md", "hs.settings.json"],
  "scripts": {
    "test": "node --test tests/compatibility/*.test.mjs",
    "validate:skills": "node scripts/validate-skills.mjs",
    "regen:agents": "node scripts/generate-agents.mjs --target claude --out agents"
  }
}
